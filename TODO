Mirar el tokenizer una vez mas para gestionar estos casos;
ls ; pwd
ls \ -l
echo hello \
ls && pwd
ls || pwd
(ls)
(ls && pwd) || echo "fail"
ls && (pwd || echo "fail")
( (ls) )
ls | (grep a && echo b)
TODOS ESTOS EL TOKENIZER DEBERIA LANZAR SYNTAX ERROR









Mirar este test ">>>>>>>>>" ya que el parser tiene que lanzar error especial asi que
el tokenizeer me tiene que mandar la string original, no solo >>