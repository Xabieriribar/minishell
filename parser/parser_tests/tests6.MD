WORD HEREDOC WORD APPEND WORD (command: cat << EOF >> log.txt)
Result:
typedef struct s_node {
    t_type node = CMD_NODE
    t_node *left = NULL
    t_node *right = NULL 
    t_node *child = NULL
    char **args = ["cat", NULL]
    t_redir redir = Linked list with two nodes, the first one being << and EOF and the second one beign >> with log.txt
}
