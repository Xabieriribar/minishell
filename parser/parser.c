#include "../includes/minishell.h"

char **create_args(t_token **tokens)
{
    t_token *temp;
    char **args;

    temp = *tokens;
    while (temp)
    {
        temp->value[i] =
    }

    return (args);

}